# ===================================
# PAYLOAD CMS - VARIABLES D'ENVIRONNEMENT
# ===================================

# Secret utilisé par Payload pour signer les tokens JWT et chiffrer les données sensibles
# Générez une valeur aléatoire sécurisée (ex: openssl rand -base64 32)
PAYLOAD_SECRET=your-secret-key-here-change-me

# URL de connexion PostgreSQL (fournie automatiquement par Railway en production)
# Format: postgres://user:password@host:port/database
DATABASE_URL=postgres://user:password@localhost:5432/webgenius_cms

# URL publique de votre instance CMS (utilisée pour générer les liens dans l'admin)
# En local: http://localhost:3000
# En production: https://votre-app.up.railway.app
PUBLIC_URL=http://localhost:3000

# Origines autorisées pour CORS/CSRF (séparer par des virgules)
# Listez tous les domaines de vos frontends qui appelleront cette API
# Exemples: https://mon-site-a.vercel.app,https://mon-site-b.ionos.fr
FRONTEND_ORIGINS=http://localhost:3000,http://localhost:3001

# Port d'écoute du serveur (défaut: 3000)
# Railway définit automatiquement cette variable en production
PORT=3000
